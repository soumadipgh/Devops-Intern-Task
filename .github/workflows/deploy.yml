name: Django CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install flake8

      - name: Lint code
        run: flake8 . --ignore=E501,E302,E303,E402,W293,F401 --exclude=migrations,venv

      - name: Run tests
        env:
          SECRET_KEY: testkey
          DEBUG: True
        run: python manage.py test

      - name: Build Docker Image
        run: docker build -t devops-task .
